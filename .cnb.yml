on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: node:20
    steps:
      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Deploy to Pages
        uses: cnb/pages@v1
        with:
          dir: dist
